{
	"name": "amaranth-tweaks",
	"version": "1.2.0",
	"description": "Usability tweaks for the Okestro groupware: bigger popups, un-clipped approval forms, a check-in badge, and a persistent login.",
	"license": "MIT",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run build.ts",
		"typecheck": "tsc --noEmit",
		"lint": "bun run build && web-ext lint --source-dir build/firefox",
		"dev:firefox": "bun run build && web-ext run --source-dir build/firefox",
		"dev:chromium": "bun run build && web-ext run --target chromium --source-dir build/chromium",
		"package": "bun run build && web-ext build --source-dir build/firefox --artifacts-dir dist --filename amaranth-tweaks-firefox-{version}.zip --overwrite-dest && web-ext build --source-dir build/chromium --artifacts-dir dist --filename amaranth-tweaks-chromium-{version}.zip --overwrite-dest"
	},
	"devDependencies": {
		"bun-types": "^1.4.2",
		"chrome-webstore-upload-cli": "^4.0.1",
		"typescript": "^7.0.2",
		"web-ext": "^10.6.0"
	}
}
